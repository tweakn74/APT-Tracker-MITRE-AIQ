{
  "name": "apt-tracker-mitre-aiq",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "live-server --port=5500 --no-browser .",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "prepare": "npx playwright install --with-deps || npx playwright install"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "ajv": "^8.17.1",
    "live-server": "^1.2.2"
  }
}
